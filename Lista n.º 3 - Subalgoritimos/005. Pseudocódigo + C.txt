+----------------+
|  PSEUDOCÓDIGO  |
+----------------+

ALGORÍTMO vetor_menor

FUNÇÃO REAL: verifique(VAR REAL: M[n]; INTEIRO: n)
INÍCIO
    VAR INTEIRO: i;
    VAR REAL: menor;

    menor = M[1];

    PARA i DE 2 ATÉ n FAÇA
        SE M[i] < menor 
        ENTÃO
            menor = M[i];
    FIM_PARA;

    RETORNE menor;
FIM.

// ALGORÍTMO principal

INÍCIO
    VAR INTEIRO: i, n; 
    VAR REAL: M[100], menor;

    ESCREVA("Informe o valor de n: "); 
    LEIA n;

    PARA i DE 1 ATÉ n FAÇA
        ESCREVA("Digite o valor M[",i,"]: "); 
        LEIA M[i];
    FIM_PARA;

    menor = verifique(M, n);

    ESCREVA("O menor valor é: ", menor);

FIM.


+---------------+
|	C	|
+---------------+

#include <stdio.h>
#include <locale.h>
#include <conio.h>
#include <math.h>
#define TAM 100

float verifique(){

}

main(){
    setlocale(LC_ALL, "Portuguese");
    int i = 0, n;
    float M[TAM], menor;

    printf("Informe o valor de n: ");
    scanf("%d", &n);

    for(i = 1; i<=n; i++){
        printf("Digite o valor M[%d]: ", i);
        scanf("%f", M[i]);
    }
        menor = verifique(M, n);
        printf("O menor valor é: ", menor);
}
